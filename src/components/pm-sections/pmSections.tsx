import React from 'react'

const PMSections = ({ children }: any) => {
    return (
        <div className="max-w-7xl py-12 px-12 m-auto">
            {children}
        </div>
    )
}

export default PMSections;
